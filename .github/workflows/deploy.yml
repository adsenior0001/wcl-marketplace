name: Build and Push to Azure Container Registry

on:
  push:
    branches:
      - main

env:
  # CRITICAL: Replace 'acrwclmarketplacexyz' with the exact name of your Azure Container Registry
  ACR_NAME: wclmarketplacexyz

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      # 1. Grab the code from GitHub
      - name: Checkout Code
        uses: actions/checkout@v4

      # 2. Log into Azure using the secret JSON we stored
      - name: Azure Login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      # 3. Log into your specific Container Registry
      - name: ACR Login
        run: az acr login --name ${{ env.ACR_NAME }}

      # 4. Build and Push the Product Service
      - name: Build and Push Product Service
        run: |
          docker build -f marketplace-infra/docker/Dockerfile.product -t ${{ env.ACR_NAME }}.azurecr.io/product-service:latest .
          docker push ${{ env.ACR_NAME }}.azurecr.io/product-service:latest

      # 5. Build and Push the Order Service
      - name: Build and Push Order Service
        run: |
          docker build -f marketplace-infra/docker/Dockerfile.order -t ${{ env.ACR_NAME }}.azurecr.io/order-service:latest .
          docker push ${{ env.ACR_NAME }}.azurecr.io/order-service:latest

      # 6. Build and Push the User Service
      - name: Build and Push User Service
        run: |
          docker build -f marketplace-infra/docker/Dockerfile.user -t ${{ env.ACR_NAME }}.azurecr.io/user-service:latest .
          docker push ${{ env.ACR_NAME }}.azurecr.io/user-service:latest

      # 7. Build and Push the API Gateway
      - name: Build and Push API Gateway
        run: |
          docker build -f marketplace-infra/docker/Dockerfile.gateway -t ${{ env.ACR_NAME }}.azurecr.io/api-gateway:latest .
          docker push ${{ env.ACR_NAME }}.azurecr.io/api-gateway:latest

      # 8. Build and Push the Frontend Angular SPA
      # (Assuming standard Angular Dockerfile location and context)
      - name: Build and Push Frontend
        run: |
          docker build -f marketplace-frontend/Dockerfile -t ${{ env.ACR_NAME }}.azurecr.io/frontend:latest marketplace-frontend
          docker push ${{ env.ACR_NAME }}.azurecr.io/frontend:latest
